# build output
dist/
# generated types + astro cache
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# environment
.env
.env.production
# macOS / editor
.DS_Store

# local tooling (not part of the published site) — regenerable via `npx skills`
.claude/
.agents/
skills-lock.json
